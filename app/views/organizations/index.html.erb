<h1>Organisationen</h1>
<%= link_to 'Neues Organ' , new_organization_path%>
<table>
  <tr>
    <th>Name</th>
    <th>Beschreibung</th>
    <th colspan="3">Optionen</th>
  </tr>

  <% @organizations.each do |organization| %>
    <tr>
      <td><%= organization.name %></td>
      <td><%= organization.description %></td>
      <td><%= link_to 'Anzeigen', organization_path(organization) %></td>
      <td><%= link_to 'Bearbeiten', edit_organization_path(organization) %></td>
      <td><%= link_to 'Löschen', organization_path(organization),
              method: :delete,
              data: { confirm: 'Bist du dir sicher???' } %></td>
    </tr>
  <% end %>
</table>
